# สรุปโปรเจกต์ Study Focus (เว็บจับเวลาอ่านหนังสือ)

เว็บแอปพลิเคชันสำหรับจับเวลาทำสมาธิ/อ่านหนังสือ ที่มาพร้อมกับระบบจัดการสมาธิและกึ่งเกม (Gamification) สร้างด้วย React (Vite) และใช้ Firebase สำหรับจัดการข้อมูล

## ✨ ฟีเจอร์หลัก (Features)

- **🍅 นาฬิกาจับเวลา Pomodoro**: ระบบจับเวลาโฟกัสและพักเบรค
- **✅ รายการสิ่งที่ต้องทำ (To-Do List)**: จดบันทึกและขีดฆ่าสิ่งที่ทำเสร็จแล้วในแต่ละช่วงเวลา
- **🎮 ระบบเก็บเลเวล (Gamification System)**:
  - ได้รับค่าประสบการณ์ (EXP) และ เหรียญ (Coins) เมื่อตั้งใจโฟกัสจนหมดเวลา
  - นำเหรียญไปเข้าร้านค้า (Pet Shop) เพื่อปลดล็อคสัตว์เลี้ยงหรือต้นไม้มาประดับหน้าจอ
  - ปลดล็อคธีมพื้นหลังเคลื่อนไหวสวยๆ เช่น ธีมไซเบอร์พังค์, ห้องสมุด, หรือธีมเทศกาลไทย
- **🏆 ระบบความสำเร็จ (Achievements)**: แจกตราประทับเมื่อสะสมเวลาครบกำหนด
- **👥 ห้องติวออนไลน์ (Multiplayer Rooms)**:
  - สร้างห้องหรือกรอกรหัสเข้าร่วมห้องกับเพื่อนได้
  - เห็นสถานะการโฟกัสของเพื่อนๆ แบะสัตว์เลี้ยงของเพื่อนในห้องแบบเรียลไทม์
- **⚙️ แผงผู้ดูแลระบบ (Admin Panel)**: หน้าควบคุมเฉพาะแอดมิน (สามารถซ่อนได้) เอาไว้เพิ่มสัตว์เลี้ยง, แก้ไขเหรียญของผู้ใช้รายคน, หรือเพิ่มธีมได้โดยตรงไม่ต้องแก้โค้ดเพิ่ม
- **📱 รองรับ PWA**: สามารถกด "Add to Home Screen" เพื่อติดตั้งลงในมือถือหรือคอมพิวเตอร์ ทำงานคล้ายแอปพลิเคชันแยกได้เลย

---

## 🏗️ โครงสร้างเทคโนโลยีที่ใช้

- **Frontend**: React.js (Vite)
- **Styling**: Vanilla CSS (เน้นความสวยงามสไตล์ Glassmorphism)
- **Database / Backend**: Firebase Firestore (ฐานข้อมูลเรียลไทม์)
- **Authentication**: Firebase Auth (ล็อกอินด้วย Google)
- **Hosting**: Firebase Hosting 
- **PWA**: `vite-plugin-pwa`

---

## � วิธีตั้งค่าและการติดตั้ง (Setup Guide)

หากต้องการนำไปรันในเครื่องตัวเอง ให้ทำตามขั้นตอนดังนี้:

### 1. โคลนสคริปต์ลงมา (Clone & Install)
เปิด Terminal ในโฟลเดอร์โปรเจกต์แล้วรันคำสั่ง:
```bash
npm install
```

### 2. การเชื่อมต่อ Firebase (รหัสผ่านฐานข้อมูล)
เพื่อความปลอดภัย ข้อมูล API Key จะไม่ถูกอัปโหลดขึ้น GitHub คุณต้องสร้างโปรเจกต์ Firebase ของคุณเอง

1. ไปที่ [Firebase Console](https://console.firebase.google.com/) แล้วสร้างโปรเจกต์ใหม่
2. สร้าง Web App แล้วเปิดใช้งานฟีเจอร์ **Firestore**, **Authentication** (Google), และ **Hosting**
3. สร้างไฟล์ชื่อว่า `.env` ไว้ที่โฟลเดอร์นอกสุดของโปรเจกต์ (ระดับเดียวกับ `package.json`)
4. คัดลอกค่าคอนฟิกจากหน้าเว็บ Firebase มาใส่ในไฟล์ `.env` ตามรูปแบบนี้:

```properties
VITE_FIREBASE_API_KEY=ใส่_api_key_ของคุณ
VITE_FIREBASE_AUTH_DOMAIN=ใส่_auth_domain_ของคุณ
VITE_FIREBASE_PROJECT_ID=ใส่_project_id_ของคุณ
VITE_FIREBASE_STORAGE_BUCKET=ใส่_storage_bucket_ของคุณ
VITE_FIREBASE_MESSAGING_SENDER_ID=ใส่_sender_id_ของคุณ
VITE_FIREBASE_APP_ID=ใส่_app_id_ของคุณ
VITE_FIREBASE_MEASUREMENT_ID=ใส่_measurement_id_ของคุณ
```
*(ไฟล์ `.env` จะถูกละเว้นโดย Git อัตโนมัติ เพื่อป้องกันรหัสรั่วไหล)*

### 3. รันโปรเจกต์ (Run Local Server)
```bash
npm run dev
```
ตัวเว็บจะรันขึ้นมาที่ `http://localhost:5173`

---

## 🛠️ แผงควบคุม (Admin Panel)

ระบบมีหน้าจัดการสำหรับแอดมินซ่อนอยู่ 
- **วิธีเข้าถึง**: กดไอคอน "ฟันเฟือง (Settings)" มุมขวาบน -> เลือก "Open Admin Panel"
- **การใช้งาน**: คุณสามารถเพิ่ม/ลบ สัตว์เลี้ยง, ธีม, ความสำเร็จ รวมถึงจัดการ Coin/Level ของผู้ใช้คนอื่น (โดยใช้ UID) ได้จากหน้านี้
- **ข้อควรระวัง (Security)**: ในสภาพแวดล้อมใช้งานจริงควรเพิ่มการป้องกันสิทธิ์ของแอดมินระดับ Database ในเมนู Firestore Security Rules

---

## 📦 การนำเว็บขึ้นสาธารณะ (Deploy to Firebase Hosting)

เมื่อแก้ไขโค้ดเสร็จแล้ว และต้องการให้คนอื่นเข้ามาใช้งานได้:

1. บิวด์หน้าเว็บ (สร้างไฟล์ production)
```bash
npm run build
```

2. อัปโหลดขึ้น Firebase (ต้องล็อคอิน `firebase login` ก่อน)
```bash
firebase deploy --only hosting
```
